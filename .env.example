# Required. Encrypts uploaded files at rest.
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY_SECRET=

# Required. Encrypts the admin session cookie.
# Generate with: openssl rand -hex 32
NUXT_SESSION_PASSWORD=
