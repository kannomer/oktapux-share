{
  "name": "Crateyard",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "..",
    "target": "builder"
  },
  "workspaceFolder": "/workspaces/crateyard",
  "remoteUser": "root",
  "forwardPorts": [3000],
  "postCreateCommand": "pnpm install && pnpm run setup",
  "customizations": {
    "vscode": {
      "extensions": [
        "Vue.volar",
        "dbaeumer.vscode-eslint"
      ]
    }
  }
}
